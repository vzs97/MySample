apply plugin:"war"

dependencies {
      compile (
              'org.springframework:spring-webmvc:3.2.8.RELEASE',
      )

      providedCompile(
              'javax.servlet:javax.servlet-api:3.1.0'
      )
}



